{{ content() }}
{{ partial('grid/breadcumb') }}
<section class="contact-area section_padding_75">
    <div class="container">
        <div class="row">
            {{ partial('forms/login') }}
            {{ partial('grid/contact') }}
        </div>
    </div>
</section>
